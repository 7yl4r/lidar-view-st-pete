# tylar's notes file
notes

# dependencies
node_modules/

# build output
dist/
*.tsbuildinfo

# logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# editor / OS
.DS_Store
.idea/
.vscode/*
!.vscode/extensions.json
*~
*.swp

# env
.env
.env.local
.env.*.local

# python
__pycache__/
*.pyc
.venv/

# source DEM GeoTIFFs (tens of MB; regenerate/redeliver locally, keep out of
# git so the repo stays well within GitHub's size limits -- the derived
# heightfield grid baked from these, web/public/assets/terrain/dem_grid.*, is
# small enough to stay committed like the rest of public/assets/**)
/DEMs/


